<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PokeDex</title>
</head>
<body>
    <h1>PokeDex</h1>
    <p>Algumas informações das Habilidades não estão traduzidas devido a API</p>
    <% results.results.forEach(pokemon => { %>
    <div>
        <p>Nome: <a href="/pokemon/<%= pokemon.name %>"><%= pokemon.name %></a></p>
        <p>URL: <%= pokemon.url %></p>
    </div>
    <% }) %>
</body>
</html>
